<Window x:Class="VictumPOS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="VictumPOS"
        Icon="favicon.ico"
        Height="760"
        Width="1180"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource ToolbarGradientBrush}" Padding="8,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <Button ToolTip="Atras" Click="Back_Click" Style="{StaticResource ToolbarButtonStyle}">
                        <TextBlock Text="Atras" Foreground="White"/>
                    </Button>
                    <Button ToolTip="Inicio" Click="Home_Click" Style="{StaticResource ToolbarButtonStyle}">
                        <TextBlock Text="Inicio" Foreground="White"/>
                    </Button>
                    <Button ToolTip="Recargar" Click="Reload_Click" Style="{StaticResource ToolbarButtonStyle}">
                        <TextBlock Text="Actualizar" Foreground="White"/>
                    </Button>
                </StackPanel>

                <Border Grid.Column="1"
                        Background="{StaticResource ToolbarTitleBrush}"
                        CornerRadius="8"
                        Margin="10,0"
                        Padding="10,0">
                    <TextBlock x:Name="UrlText"
                               Text="Cargando..."
                               Foreground="White"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                </Border>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button ToolTip="Ver atajos de teclado" Click="Shortcuts_Click" Style="{StaticResource ToolbarButtonStyle}">
                        <TextBlock Text="Atajos" Foreground="White"/>
                    </Button>
                    <Button ToolTip="Configurar impresoras" Click="Settings_Click" Style="{StaticResource ToolbarButtonStyle}">
                        <TextBlock Text="Impresoras" Foreground="White"/>
                    </Button>
                    <Button ToolTip="Configuracion" Click="Config_Click" Style="{StaticResource ToolbarButtonStyle}">
                        <TextBlock Text="Config" Foreground="White"/>
                    </Button>
                    <Button ToolTip="Cerrar" Click="Exit_Click" Style="{StaticResource ToolbarButtonStyle}">
                        <TextBlock Text="Salir" Foreground="White"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border x:Name="NotificationBar"
                Grid.Row="1"
                Margin="12,10,12,0"
                Padding="12,10"
                HorizontalAlignment="Stretch"
                Background="#F0FDFA"
                BorderBrush="#00ACC1"
                BorderThickness="1"
                CornerRadius="8"
                Visibility="Collapsed">
            <TextBlock x:Name="NotificationText" TextWrapping="Wrap"/>
        </Border>

        <Grid Grid.Row="2">
            <wv2:WebView2 x:Name="webView"/>

            <Grid x:Name="LoadingOverlay" Background="#76071316" Visibility="Collapsed">
                <Border HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="White"
                        CornerRadius="8"
                        Padding="22,16">
                    <StackPanel Orientation="Horizontal">
                        <ProgressBar Width="120" Height="18" IsIndeterminate="True" Margin="0,0,14,0"/>
                        <StackPanel>
                            <TextBlock Text="Cargando VictumPOS" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="Sincronizando informacion..." Foreground="{StaticResource AppMutedTextBrush}" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</Window>
